import React from 'react';
import Card from '../components/Card';

const WelcomeCard = props => (
  <Card className="welcome-card simple-card-wide mdl-card mdl-shadow--2dp" {...props} />
);

export default WelcomeCard;
